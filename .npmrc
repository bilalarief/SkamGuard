# SkamGuard — npm configuration
# Prevents package-lock.json desync issues across team members and CI/CD

# Use exact versions to prevent drift between team members
save-exact=true

# Enforce engine compatibility
engine-strict=true

# Use lockfile strictly — fail if package.json and package-lock.json are out of sync
# This catches the issue BEFORE it reaches Cloud Run
package-lock=true
